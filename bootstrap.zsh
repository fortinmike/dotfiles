DOTFILES_DIR=$(dirname $0)

# Note: Make sure this repo was cloned w/ submodules!

source "$DOTFILES_DIR/shell/plugins.zsh"
source "$DOTFILES_DIR/shell/env.zsh"
source "$DOTFILES_DIR/shell/commands.zsh"
source "$DOTFILES_DIR/shell/key-bindings.zsh"

