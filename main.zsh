THIS_DIR=$(dirname $0)

# Note: Make sure this repo was cloned w/ submodules!

source "$THIS_DIR/plugins.zsh"
source "$THIS_DIR/env.zsh"
source "$THIS_DIR/commands.zsh"
source "$THIS_DIR/key-bindings.zsh"

