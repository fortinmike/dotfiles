- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: ../dotfiles/gitconfig
    ~/.ssh/config:
        path: ../dotfiles/ssh/config
        create: true
    ~/.zshrc: ../dotfiles/zshrc
    ~/.gemrc: ../dotfiles/gemrc


