COMMANDS_DIR="$ZSH_CONFIG_DIR/commands"

source "$COMMANDS_DIR/dotfiles.zsh"
source "$COMMANDS_DIR/shell-wrappers.zsh"
source "$COMMANDS_DIR/filesystem.zsh"
source "$COMMANDS_DIR/encode-decode.zsh"
source "$COMMANDS_DIR/escaping.zsh"
source "$COMMANDS_DIR/hashing.zsh"
source "$COMMANDS_DIR/host-keys.zsh"
source "$COMMANDS_DIR/aliases.zsh"
source "$COMMANDS_DIR/processes.zsh"
source "$COMMANDS_DIR/network.zsh"

unset COMMANDS_DIR
